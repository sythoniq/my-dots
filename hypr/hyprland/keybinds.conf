source=./variables.conf
$Super = SUPER

# Actions
bind = Super, B, exec, $browser
bind = Super, Return, exec, $terminal
bind = Super Ctrl, Q, killactive
bind = Super, T, togglefloating
bind = Super, W, exec, pkill waybar || waybar
bind = Super Shift, S, exec, hyprshot -m region --clipboard-only
bind = Super Shift, F, fullscreen, 1
bind = Super, A, exec, rofi -show drun
bind = Super, L, exec, hyprlock
bind = Super Shift, W, exec, waydroid show-full-ui
bind = Super Alt, W, exec, waydroid session stop
bind = Super Alt, F, exec, ~/fribbels-e7/./fribbelse7optimizer --no-sandbox
bind = Super Shift, Q, exec, hypr-zoom -interp=Linear

# Workspace
bind = Super, Tab, workspace, m+1
bind = Super Shift, Tab, workspace, m-1
bind = Alt, Tab, cyclenext
bind = Alt, Tab, bringactivetotop

# Swtich to workspace [0-9]
bind = Super, 1, workspace, 1
bind = Super, 2, workspace, 2
bind = Super, 3, workspace, 3
bind = Super, 4, workspace, 4
bind = Super, 5, workspace, 5
bind = Super, 6, workspace, 6
bind = Super, 7, workspace, 7
bind = Super, 8, workspace, 8
bind = Super, 9, workspace, 9
bind = Super, 0, workspace, 10

# Move active window and follow to workspace mainMod + SHIFT [0-9]
bind = Super SHIFT, 1, movetoworkspace, 1
bind = Super SHIFT, 2, movetoworkspace, 2
bind = Super SHIFT, 3, movetoworkspace, 3
bind = Super SHIFT, 4, movetoworkspace, 4
bind = Super SHIFT, 5, movetoworkspace, 5
bind = Super SHIFT, 6, movetoworkspace, 6
bind = Super SHIFT, 7, movetoworkspace, 7
bind = Super SHIFT, 8, movetoworkspace, 8
bind = Super SHIFT, 9, movetoworkspace, 9
bind = Super SHIFT, 0, movetoworkspace, 10

bind = Super, Z, togglespecialworkspace, ent
bind = Super Shift, Z, movetoworkspace, special:ent

bind = Super, X, togglespecialworkspace, rbx
bind = Super Shift, X, movetoworkspace, special:rbx

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = Super, mouse:272, movewindow
bindm = Super, mouse:273, resizewindow


# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1.3 @DEFAULT_AUDIO_SINK@ 10%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 10%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -n2 set 10%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -n2 set 10%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous
